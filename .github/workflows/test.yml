name: Testing

on:
  pull_request:
    branches: 
      - master
      - desarrollo

jobs:
  run-test:
    runs-on: ubuntu-latest

    steps:
      - name: Install Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.16.4
          
          
      - uses: actions/checkout@v3
      
      
      - uses: ./
